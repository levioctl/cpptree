#include "tree/tree.h"
#include "tree/node.h"

template<typename T>
treelib::Tree<T> fit_tree_to_window(treelib::Tree<T> tree,
                                    int window_size,
                                    std::shared_ptr<treelib::Node<T>> selected) {
    return tree;
}
